% output 1
syms M m l g u y ydot theta thetadot

% define the system state
x = [y; ydot; theta; thetadot];

% define system dynamics
yddot = (-m*l*sin(theta)*thetadot^2 + m*g*sin(theta)*cos(theta) + u)/...
    (M + m*sin(theta)^2);
thetaddot = 
xdot = [ydot; yddot; thetadot;
    
    ]; 
y = x1;



% define the set of parameters specific to our ODE
parameters.M = 1.0731;
parameters.l= 0.2300;
parameters.g = 9.8;